{
  "dependencies_to_add": {
    "stripe": "^18.0.0",
    "@stripe/stripe-js": "^4.0.0"
  },
  "dependencies_to_remove": {
    "note": "Remove any przelewy24 dependencies if they exist"
  },
  "installation_command": "npm install stripe @stripe/stripe-js"
}